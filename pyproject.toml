[project]
name = "advanced-zone-helper-ipc"
version = "2.0.0"
description = "KiCad IPC plugin for creating zones from shapes"
requires-python = ">=3.9"
dependencies = ["kicad-python>=0.1.0"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
